<!DOCTYPE html>
<html>
<head>
  <title>Carrito de Compra</title>
</head>
<body>
  <h1>Carrito de Compra</h1>
  {{#if cart}}
    <h2>Carrito ID: {{cart.id}}</h2>
    {{#if cart.products.length}}
      <ul>
        {{#each cart.products}}
          <li>Producto ID: {{this.id}} - Cantidad: {{this.quantity}}</li>
        {{/each}}
      </ul>
    {{else}}
      <p>No hay productos en el carrito de compra</p>
    {{/if}}
  {{else}}
    <p>No se encontró el carrito de compra</p>
  {{/if}}
</body>
</html>
